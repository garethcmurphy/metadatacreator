import { InstrumentFactory } from "./../src/InstrumentFactory";

import { expect } from "chai";
import "mocha";

describe("check insutrment factory", () => {
  it("should have property contactemail", () => {
    const result = new InstrumentFactory();
  });
});
